// Re-exports so state.ts can stay free of React while sharing one easing model.
export { rolled, roll, rollEase, lerp, clamp01, pr, fade, settle, jitter, EASE } from './anim';
export { IN, US } from './tokens';
